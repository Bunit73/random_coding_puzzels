#include <stdlib.h>
#include <stdio.h>

int main() {
	char* buffer;
	size_t result;
	long lSize;

	nameFile = fopen("names.txt");

	if(nameFile == NULL) {
		fputs("File error", stderr);
		return 1;
	}

	return 0;
}
